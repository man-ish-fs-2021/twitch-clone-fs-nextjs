import Toggle from "./Toggle";
import Wrapper from "./wrapper";

const Sidebar = () => {
  // server component
  // follow users

  return (
    <Wrapper>
      <Toggle />
    </Wrapper>
  );
};

export default Sidebar;
